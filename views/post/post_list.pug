extends ../layout 

block content

    h2 Post list

    ul
        each post in post_list
            li
                a(href=post.url) #{post.title} 
                |   - #{post.owner.getName}

                p #{post.distance} m.
        
        else
            p There are no posts near you    